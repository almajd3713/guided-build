# Task CLI Plan

Create a local task manager in Python.

1. Parse add and list commands with useful errors.
2. Persist tasks using atomic file replacement.
3. Add update and delete behavior.
4. Package the CLI and document recovery.

Use unit tests. Networking and accounts are out of scope.
