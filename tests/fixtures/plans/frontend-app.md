# Reading Tracker Plan

Build an accessible frontend that lets a user add books, update reading progress, filter the library, and retain state locally.

Start with domain behavior and tests, then add the interface, accessibility checks, and production build. Authentication, social features, and a backend are future work.
